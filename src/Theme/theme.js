import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
      mode: "dark",
      primary: {
        main: '#ffffff',
      },
      secondary: {
        main: '#ffffff',
      },
    
    },
  });

  export default theme;